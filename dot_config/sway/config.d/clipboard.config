# use wl-clipboard, clipman and rofi for clipboard management

exec wl-paste --type=text --watch ~/.config/sway/bin/clip.sh

bindsym $mod+v exec clipman pick --tool=CUSTOM --tool-args='rofi -dmenu -p "cb pick"'
bindsym $mod+c exec clipman clear --tool CUSTOM --tool-args='rofi -dmenu -p "cb clear"'
