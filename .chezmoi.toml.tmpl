{{ $email := promptString "email" -}}
{{ $brewprefix := promptString "Value of brew --prefix" -}}
[data]
    email = "{{ $email }}"
    brewprefix = "{{ $brewprefix }}"

    [data.gtk]
        theme = "Materia"
        icon.theme = "Papirus"
        font = "Roboto 10.5"
